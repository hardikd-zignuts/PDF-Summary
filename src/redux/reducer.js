const initialState = {
    status: false
}

const pdfReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return { ...state }
    }
}

export default pdfReducer